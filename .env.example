# ============================================================
# Payatas Ledger — Environment Variables
# Copy this file to .env and fill in your actual values.
# Never commit .env to version control.
# ============================================================

# Supabase Project (Vite requires VITE_ prefix for client env vars)
VITE_SUPABASE_URL="https://xyaqigazszqhvvglqint.supabase.co"
VITE_SUPABASE_ANON_KEY="sb_publishable_ftY2kTePsAkVcK-PrgTgiQ_jG636mXp"

# Direct DB connection (for migrations / local Supabase CLI use only)
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.xyaqigazszqhvvglqint.supabase.co:5432/postgres"

# App
APP_ENV="development"   # development | production
APP_URL="http://localhost:3000"

# Sentry (optional — leave blank to disable)
VITE_SENTRY_DSN="https://examplePublicKey@o0.ingest.sentry.io/0"

# Prerender (optional — set to 1 to skip Puppeteer prerendering)
# Automatically skipped on Vercel (VERCEL=1) where Chrome libs are missing
# NO_PRERENDER=1
